name: hello_flutter
description: A simple chat app with Flask backend (stable build)
publish_to: 'none'

version: 2.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^1.2.2
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  dio: ^5.7.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
