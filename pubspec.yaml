name: hello_flutter
description: A simple chat app with Flask backend (v2.2)
publish_to: 'none'

version: 2.2.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  shared_preferences: ^2.2.3
  intl: ^0.19.0
  dio: ^5.7.0
  file_picker: ^8.0.3
  path_provider: ^2.1.4
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
