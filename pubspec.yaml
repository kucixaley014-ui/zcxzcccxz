name: flappy_chat
description: Simple chat client
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.6
  shared_preferences: ^2.0.15
  file_picker: ^5.2.2
  intl: ^0.18.0
